# SPIKE RESULTS — FNB-96526 verdicts

This file is the **acceptance target**. Every ⚠️ cell from the FNB-96526 report must end up here
with a concrete verdict (✅ works / ❌ does not / ⚠️ partial) backed by **evidence** (screenshot path,
log line, build error, or dashboard link). "Docs say so" is NOT evidence — only observed behaviour.

Legend: ✅ confirmed working · ❌ confirmed not working (with evidence) · ⚠️ partial / caveat · ⬜ not yet run

## A. BASELINE captures (Embrace getting-started — must be DEMONSTRATED, not just assumed ✅)
Requires dual-export → real data on the **Embrace cloud dashboard** + Grafana.

> **UPDATE 2026-06-29 — dual-export to Embrace cloud DONE for all 4 clients** (the 2026-06-26
> "Grafana-only" decision was reversed). The "screenshot the Embrace cloud dashboard" sub-AC is now
> SATISFIED: per-case + app-UI captures live in `screenshots/embrace/<web|ios|android|flutter>/`
> (App IDs Web `ctac2` · iOS `gq23k` · Android `2tbxs` · Flutter `tzb7f`). The ✅ verdicts below remain
> **Grafana-verified** (the F1/F2 findings are about the self-host path); the Embrace-cloud captures are the
> baseline-AC evidence. **For a consolidated "what self-host/Grafana-only loses vs Embrace cloud" table see
> [`SELFHOST_GAP_ANALYSIS.md`](./SELFHOST_GAP_ANALYSIS.md).**

| ID | Capture | Web (Ng8) | Web (Ng20) | Android | iOS | Flutter | Evidence |
|----|---------|:--:|:--:|:--:|:--:|:--:|---|
| B1 | Real error (JS / native unhandled) | ❌ | ✅ | ✅ | ✅ | ✅ | **Android:** unhandled `RuntimeException` → Tempo `crash` STATUS_ERROR + Loki ERROR (raw, debug-unminified). **iOS:** `caught_error` ERROR log; unhandled crash = same KSCrash raw path proven on Flutter-iOS. Web(Ng20): unhandled `DemoUnhandledError` → Tempo span `crash` status=STATUS_CODE_ERROR w/ `exception` event + JS stacktrace (traceID `865ae808…`); Loki log `sys.exception` `emb_exception_handling=unhandled_error`, severity ERROR. **Flutter:** both arms. Embrace arm — unhandled Dart `StateError` captured by guarded zone → Loki `Bad state: EmbraceGrafanaDemo intentional unhandled crash (action.name=crash)` (service `io.embrace.demo.embraceDemoFlutter:Runner`); native log `[ERROR:…dart_vm_initializer] Unhandled Exception` w/ Dart stack. OTel arm — same error via `runZonedGuarded` → Loki, `detected_level=error`. RAW Dart string, not symbolicated (F2). |
| B2 | Performance / Web Vitals | ❌ | ✅ | ✅ | ✅ | ✅ | **Android:** `delay` 761ms span (Tempo). **iOS:** `delay` 1221ms span + Embrace auto startup spans. Web(Ng20): Embrace auto-captures Web Vitals — Loki `emb_type=ux.web_vital` … `documentLoad` span ~36 ms. (OTel arm: none — see F1b.) **Flutter:** `delay` performance span confirmed in Tempo, durationMs≈1870 for an ~1.86 s artificial delay (OTel arm, trace `1c2b619e…`); Embrace arm `delay` span also present. Embrace additionally auto-emits startup spans (see E5). |
| B3 | Session / user timeline | ❌ | ✅ | ⚠️ | ✅ | ✅ | **Android (OTel arm):** action logs form a timeline but no Embrace session grouping ⚠️. **iOS:** Embrace `emb-session` + `emb-screen-view` + `emb-process-launch` auto. Web(Ng20): Tempo root span `emb-session-part` … breadcrumbs. **Flutter:** Embrace arm auto-emits `emb-session` (3) + `emb-screen-view` spans, plus our `addBreadcrumb` calls; OTel arm breadcrumbs land as Loki INFO `breadcrumb: tapped: <action>` (`event.kind=breadcrumb`). |
| B4 | Custom event | ❌ | ✅ | ✅ | ✅ | ✅ | **Android & iOS:** `workflow` parent + `capture`/`save`/`sync` children w/ step.name/status/data + events; failed sync → STATUS_ERROR + http.status=503 + exception.*. Web(Ng20): Loki INFO `demo.custom_event` + `workflow` span events. **Flutter:** `workflow` parent span + `capture`→`save`→`sync` children with `step.name`/`step.status`/`step.data` and timestamped events (`started`/`captured`/`saved`/`synced`/`completed`); failed `sync` → span `STATUS_CODE_ERROR`, `step.status=failure`, `http.status=503`, `exception.type`/`exception.message`. Verified BOTH arms (OTel trace `5bd690a5…`; Embrace trace `9a3f2768…`). |

## B. Embrace spikes (⚠️ in report → resolve here)
| ID | Question | Platform(s) | Verdict | Evidence |
|----|----------|-------------|:--:|---|
| E1 | No-account init works without App ID? (does OTLP still reach Grafana?) | Android, Flutter | Flutter: ✅ · Android: ⚠️ | **Android:** ⚠️ CONDITIONAL (runtime-verified) — Embrace 9.0 `start()` with NO app_id THROWS `IllegalArgumentException: "No appId supplied in embrace-config.json … unless you configure an OTel exporter and add embrace.disableMappingFileUpload=true to gradle.properties"` (logcat). So Android no-account needs (a) an OTel exporter + (b) that gradle flag — same mechanism iOS uses. The app still streamed all demo telemetry to Grafana via the OTel-Java arm (`telemetry.tool=embrace`). The build also required bumping Kotlin→2.3.0 (Embrace 9.0 metadata). **Flutter (iOS):** YES. EmbraceIO 6.20.0 has an appId-less `Embrace.Options(export: OpenTelemetryExport(...))` init (`appId:String?`; validation throws only `"OpenTelemetryExport must be provided when not using an appId"` if BOTH appId+export are nil). Wired in `AppDelegate.swift` with a custom OTLP/JSON exporter → `EMBRACE-DEMO: native setup OK (no-account, custom OTLP export)`, app `exporting=true`, demo + Embrace startup/session spans reached our Grafana with NO Embrace account. **Caveat:** Dart `addSpanExporter`/`addLogRecordExporter` are NO-OPS on iOS (plugin returns nil; Dart `start()` only attaches to an already-started native client) → iOS export MUST be configured natively. Embrace also overrides `service.name`→`io.embrace.demo.embraceDemoFlutter:Runner` and drops contract attrs. (Android arm not built — no toolchain.) |
| E2 | Crash → Grafana: recorded? **symbolicated or raw? grouping?** | iOS, Android, Flutter | Flutter/iOS/Android: ⚠️ raw | **Android:** unhandled crash → Grafana RAW (debug-unminified; a release/R8 build would arrive obfuscated with no de-obfuscation on the OTLP→self-host path — mapping upload + symbolication are Embrace-backend features). **iOS:** raw, no grouping (KSCrash). **Flutter (iOS):** Recorded, RAW, no grouping on the OTLP→Grafana path. The unhandled Dart `StateError` reached Loki via Embrace as `Bad state: EmbraceGrafanaDemo intentional unhandled crash (action.name=crash)` + native `dart_vm_initializer` Unhandled-Exception log with the Dart stack. It is **raw/unsymbolicated** (server-side symbolication is an Embrace-backend feature, absent on the self-host path — see F2) and Grafana/Loki/Tempo do **no crash grouping** (that's also a backend feature). NB this is a Dart unhandled-error path (KSCrash handles native signals); a true native crash would upload on next launch likewise raw. |
| E3 | ANR / App Hang reported? | Flutter / Android | Android: ✅ · iOS: ⚠️ | **Android:** a 6 s main-thread block (real ANR condition) → `anr` span (6001ms) + log reach Grafana on the self-host path; Embrace's native ANR detector (`anr_info`) is documented ✅ for Android. The "ANR" concept is Android-specific; iOS equivalent is **app-hang**. Android arm not built (no toolchain), so the native Embrace ANR signal is **not demonstrated**. What IS shown on iOS: a 6 s main-isolate block produces our `anr` span in Tempo (durationMs≈6006, trace `9736da3e…`). Embrace's native app-hang detector is configured (`.addDefaults()` capture services + KSCrash) but a discrete hang event was not separately surfaced in this run. Verdict pending an Android run for the true ANR signal. |
| E4 | Slow / frozen frames metric emitted? | iOS, Android, Flutter | iOS/Android: ⚠️ | **iOS & Android:** a `frames` jank action (12×120ms main-thread bursts) is captured as a MANUAL span, but neither SDK auto-emitted a dedicated slow/frozen-frames METRIC without explicit instrumentation (matches the report's ⚠️). Flutter not separately exercised. |
| E5 | App startup cold/warm auto-measured (Moments removed v3)? | Flutter | ✅ | **YES — auto-measured, no manual code.** Moments were removed in Embrace v3; the Traces API auto-instruments startup. Observed in Tempo (Embrace arm, no manual instrumentation): `emb-app-startup-cold` (1), `emb-app-startup-warm` (2), `emb-time-to-first-frame-flutter`, `emb-FlutterViewController-time-to-first-render`, `emb-session`. So startup cold/warm is captured automatically by the Embrace Flutter SDK. |
| E6 | Session replay exists in SDK at all? | all | ❌ | **No session-replay API in any Embrace client SDK examined.** Grep of the Embrace Web SDK (`@embrace-io/web-sdk`) and the iOS `EmbraceIO` pod surfaces found no session-replay capability (and none in the Android/Flutter SDK APIs). Embrace markets replay as a newer/SaaS-side capability; absent from these client SDKs → matches the report's "almost certainly no". |
| E7 | Handled exception (try/catch→log) grouped like unhandled? | iOS, Web | Web: ✅ · iOS: ✅ | **iOS:** handled try/catch error logged as ERROR (`demo handled exception (action.name=caught_error)`), captured like an unhandled one — same as Web. Web(Ng20): YES — same capture path. Handled `log.logException(e,{handled:true})` and the unhandled crash both land as Loki `emb_type=sys.exception` records with identical structure (severity ERROR, `exception_name`, full `exception_stacktrace`). The ONLY differentiator is the `emb_exception_handling` label: `handled` vs `unhandled_error`. So they are grouped/captured the same, distinguished by one attribute. (iOS: ⬜) |
| E8 | Web framework fit — SDK embeds & runs? | Angular 8, Angular 20, Electron | Ng20: ✅ · Ng8: ❌ | Web(Ng20): YES. `@embrace-io/web-sdk@2.22.0` core is framework-agnostic; manually wired into Angular 20 (no React, the `react` dep is peer-only and the react-instrumentation sub-export is unused). `ng build` succeeds (363.51 kB raw / 96.32 kB gzip) and real telemetry reaches Grafana: action spans (`delay`/`crash`/`caught_error`/`workflow`), `emb-session-part`, `documentLoad`, Web Vitals. **Ng8: ❌** — `npm i @embrace-io/web-sdk@2.22.0` installs but `ng build` fails (exit 1, 49 TS errors): the SDK's modern `@opentelemetry/*` typings use `import type` (TS 3.8) while Angular 8 hard-caps TypeScript <3.6 (skipLibCheck no help; TS upgrade blocked by `@angular/compiler-cli@8`). Electron 8 / Node 14 inherit the same TS ceiling → ❌. Realistic fallback = plain-OTel pinned to OTel 1.x (loses Embrace auto-RUM); strategic fix = migrate WebPOS off Angular 8 to the Ng20 ✅ path. Evidence: `docs/_verdicts/angular8.md` + `clients/web-angular8/evidence/ng-build-embrace-FAIL.log`. (Electron: ⬜) |

## C. plain-OTel comparison (proves F1: Embrace ≈ OTel for the Grafana path)
| ID | Question | Platform(s) | Verdict | Evidence |
|----|----------|-------------|:--:|---|
| F1a | Does plain OTel SDK deliver the SAME signals to Grafana as Embrace (no-account)? | Android/Flutter/Web | Web: ✅ · Flutter: ✅ | Web(Ng20): YES for the demo signals … OTel even preserved the exact `exception.type=DemoHandledError`. **Flutter:** YES. The `TELEMETRY_TOOL=otel` arm (workiva `opentelemetry` 0.18.11 + manual OTLP logs) delivered the SAME demo signals to the SAME collector — `workflow`+capture/save/sync tree with status=ERROR/`http.status=503` on failed sync, `delay` perf span, breadcrumbs+caught-error+crash logs. **OTel arm is actually CLEANER for a shared dashboard:** it honors ALL SCHEMA_CONTRACT keys exactly (`service.name=embrace-demo-flutter`, `telemetry.tool=otel`, `user.id`, `device.model=iPhone18,3`, `os.version=iOS 26.5`, `app.version=1.0.0+1`), whereas the Embrace arm overrides `service.name` and drops those keys. |
| F1b | What (if anything) does Embrace add over OTel when target = Grafana? | all | Web: ⚠️ | Web(Ng20): Embrace adds **auto-instrumentation that bare OTel does not**: Web Vitals (`ux.web_vital`), user-timing (`ux.user_timing`), long-animation-frames (`ux.loaf_scripts`), first-interaction, click tracking, and a session timeline (`emb-session-part`) — all observed in Grafana from the Embrace arm and ABSENT from the OTel arm (which only had what we hand-instrumented + a manual `window.onerror` shim for B1). For the raw spans/logs themselves the two are equivalent on the Grafana path; Embrace's value-add is the built-in browser RUM signals + session grouping, not the transport. |
| F2  | Is crash/ANR symbolication LOST on the OTLP→Grafana path (server-side only)? | mobile | Flutter/iOS/Android: ✅ (lost) | **Confirmed LOST on all 3 mobile platforms.** Android/iOS/Flutter crashes reach Grafana as RAW stacks; no symbolication/de-obfuscation or crash-grouping on the OTLP→self-host path (those are Embrace cloud-backend features). **Flutter (iOS):** Confirmed LOST. The Embrace arm's crash reached Grafana as a RAW Dart error string + raw stack (`Bad state: …`, `dart_vm_initializer Unhandled Exception`); there is no dSYM/symbol upload step on the OTLP→self-host path and Loki/Tempo perform no symbolication or crash-grouping. Symbolication + grouping are Embrace **cloud-backend** features that do not exist when exporting OTLP to our own Grafana. (Same expectation for native crashes / Android — backend-side only.) |

## D. Ops / constraints (documented, not button-demoable)
- **O1** Sentry self-hosted footprint (~72 svc, 4 vCPU/16 GB+swap, FSL) — out of scope unless D4 flips.
- **O2** Grafana ↔ existing ClickHouse reuse — note feasibility only.
- **O3** iOS allows 1 crash handler → Embrace/Sentry vs incumbent Crashlytics — document.

---
_Last updated: 2026-06-29 — all 4 client platforms (Web Ng20 / Flutter / iOS / Android) built & verified
in Grafana; Angular 8 spike ❌; E1–E8 + F1/F2 resolved. Dual-export to the Embrace cloud dashboard now
DONE for all 4 clients (per-case + app-UI screenshots in `screenshots/embrace/`). Self-host gap table:
`SELFHOST_GAP_ANALYSIS.md`._
